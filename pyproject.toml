[project]
name = "agentic-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12.12"
dependencies = [
    "click>=8.3.1",
    "faster-whisper>=1.2.1",
    "llm>=0.28",
]

[project.scripts]
recording-processor = "recording_recording_processor:cli"
